import { TutorfilterPipe } from './tutorfilter.pipe';

describe('TutorfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TutorfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
