import { FieldsfilterPipe } from './fieldsfilter.pipe';

describe('FieldsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
