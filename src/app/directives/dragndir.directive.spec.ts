import { DragndirDirective } from './dragndir.directive';

describe('DragndirDirective', () => {
  it('should create an instance', () => {
    const directive = new DragndirDirective();
    expect(directive).toBeTruthy();
  });
});
